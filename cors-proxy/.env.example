# 讯飞 MaaS API Key
# 格式通常为: appid:secret（不带 Bearer 前缀）
# 此文件用于 CORS 代理服务器启动时自动加载 Key
# 配置后无需在浏览器中再次输入
#
# 安全提示： 
#   - 此文件不应提交到 Git 仓库（已加入 .gitignore）
#   - 如果愿意每次都从浏览器输入 Key，可以不设此文件
#
# XFYUN_API_KEY=your_appid:your_secret
